rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Allow read access to all documents for any user
    match /{document=**} {
      allow read;
      // Allow write access only for authenticated users
      allow write: if request.auth != null;
    }
  }
}
